spring.application.name=${SPRING_APP_NAME:securityUsingStarterBuildingApps}
logging.level.org.springframework.security=${SPRING_SECURITY_LOG_LEVEL:TRACE}
logging.pattern.console = ${LOGPATTERN_CONSOLE:%green(%d{HH:mm:ss.SSS}) %blue(%-5level) %red([%thread]) %yellow(%logger{15})-%msg%n}
#debug=true

spring.config.import=application_production.properties
spring.profiles.active = default

spring.datasource.url=jdbc:postgresql://${DATABASE_HOST:localhost}:${DATABASE_PORT:5432}/${DATABASE_NAME:securitydb}
spring.datasource.username=${DATABASE_USERNAME:securityuser}
spring.datasource.password=${DATABASE_PASSWORD:mypassword}
spring.jpa.show_sql=${JPA_SHOW_SQL:true}
spring.jpa.properties.hibernate.format_sql=${HIBERNATE_FORMAT_SQL:true}
spring.datasource.driver-class-name=org.postgresql.Driver
#spring.jpa.database-platform=org.hibernate.dialect.PostgreSQLDialect

sever.servlet.session.timeout=${SESSION_TIMEOUT:125s}
spring.security.oauth2.resourceserver.jwt.jwk-set-uri=${JWK_SET_URI:http://127.0.0.1:8180/realms/springSecurityDev/protocol/openid-connect/certs}


#spring.security.oauth2.resourceserver.opaque.introspection-uri=${INTROSPECT_URI:http://127.0.0.1:8180/realms/springSecurityDev/protocol/openid-connect/token/introspect}
#spring.security.oauth2.resourceserver.opaque.introspection-client-id=${INTROSPECT_ID:springSecOauth2Introspect}
#spring.security.oauth2.resourceserver.opaque.introspection-client-secret=${INTROSPECT_SECRET:3spLLNhLvH31TXuOMMEaxNZRiX4MJe9d}
##
#spring.security.oauth2.resourceserver.opaque.introspection-uri=${INTROSPECT_URI}
#spring.security.oauth2.resourceserver.opaque.introspection-client-id=${INTROSPECT_ID}
#spring.security.oauth2.resourceserver.opaque.introspection-client-secret=${INTROSPECT_SECRET}










